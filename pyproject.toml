[project]
name = "miniflux-bot"
version = "1.0.0"
requires-python = ">=3.14"
dependencies = [
    "aiogram==3.29.0",
    "miniflux==1.1.6",
    "python-dotenv==1.2.2",
    "requests==2.34.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
